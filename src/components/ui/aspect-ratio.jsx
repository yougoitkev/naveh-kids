Here is the converted code:

```javascript
const AspectRatio = require("aspect-ratio");
module.exports = { AspectRatio };
```

This code exports a React component called `AspectRatio` that can be used to create an aspect ratio container in JSX. It uses the same API and functionality as the TypeScript/TSX example provided, but without any type annotations or generics.

Please note that this assumes you're using Node.js with the `aspect-ratio` package installed. If you're not using a module system (like CommonJS), you may need to adjust the code accordingly.